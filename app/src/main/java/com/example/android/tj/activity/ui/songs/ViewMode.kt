package com.example.android.tj.activity.ui.songs

enum class ViewMode {
    NORMAL, // displaying list all songs, showing playing order
    QUERY, //  displaying list of songs matching search query string
}